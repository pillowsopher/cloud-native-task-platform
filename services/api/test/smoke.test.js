import { test } from 'node:test';
import assert from 'node:assert/strict';

// Placeholder so `npm test` / CI has something real to run. Extend with
// supertest + a disposable test-DB container once endpoints grow.
test('module loads without throwing', () => {
  assert.ok(true);
});
