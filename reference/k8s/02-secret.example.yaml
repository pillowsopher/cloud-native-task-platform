# Copy to 02-secret.yaml, fill in real values, and DO NOT commit the real file.
# kubectl create secret generic task-secrets -n task-platform \
#   --from-literal=DATABASE_URL='postgres://postgres:CHANGEME@postgres:5432/tasks' \
#   --from-literal=POSTGRES_PASSWORD='CHANGEME'
apiVersion: v1
kind: Secret
metadata:
  name: task-secrets
  namespace: task-platform
type: Opaque
stringData:
  DATABASE_URL: "postgres://postgres:CHANGEME@postgres:5432/tasks"
  POSTGRES_PASSWORD: "CHANGEME"
