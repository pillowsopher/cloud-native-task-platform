node_modules/
.env
*.log
*.pem

# Python
**/venv/
**/__pycache__/
**/*.pyc
**/.pytest_cache/
**/test.db

# Local tool installers, not project source
dependencies/

# Local-only answer-key scaffold, not part of the actual project
/reference/

# Claude Code local session/config data
.claude/

# Terraform
**/.terraform/
**/*.tfstate
**/*.tfstate.backup
**/.terraform.lock.hcl
**/*.tfvars
**/backend.hcl

# Lambda build artifacts
**/lambda/**/function.zip
**/lambda/**/node_modules/
**/terraform/**/*.zip

# k8s real secrets (keep only the .example.yaml in git)
**/k8s/02-secret.yaml

# Local-only workaround for Docker Desktop Kubernetes on this machine, not portable
/scripts/k8s-load-image.ps1
